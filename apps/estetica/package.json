{
  "name": "healthy-smile-estetica",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test tests/*.test.mjs && node tests/cloud-client.cjs && node tests/alignment-points.cjs",
    "configure": "node scripts/configure.mjs",
    "check:config": "node scripts/check-config.mjs",
    "dev": "wrangler dev --config wrangler.local.json",
    "deploy": "npm run check:config && wrangler deploy --config wrangler.local.json",
    "db:migrate": "wrangler d1 migrations apply DB --remote --config wrangler.local.json",
    "postinstall": "node scripts/prepare-pose-assets.mjs"
  },
  "dependencies": {
    "jose": "^6.1.0"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
